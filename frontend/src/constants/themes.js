// File path: code_tutor2/frontend/src/constants/themes.js

// Définition des thèmes disponibles
export const themes = {
  dark: "dark",
  light: "light",
  system: "system",
};

// État initial pour le contexte de thème
export const initialThemeState = {
  theme: themes.system,
  setTheme: () => null,
};
